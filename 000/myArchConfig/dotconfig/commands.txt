inxi -F                               #hardware info




sudo pacman-key --init                 #below
sudo pacman-key --populate archlinux   #What I can not check now anymore is that 
                                       #reinstalling the archlinux-keyring would probably 
                                       #have fixed this without initializing and populating
                                       #it. Will try that next time.

pacman-key --populate                  # root                                       



sudo pacman-key --refresh-keys         #If necessary this one



sudo pacman -Sy archlinux-keyring      #update keyring
                                       #Fix "invalid or corrupted package 
                                       #(PGP signature)" error in Arch Linux 



sudo pacman -Syu haveged               #haveged.service  In addition, we recommend
sudo systemctl start haveged           #installing haveged, a daemon that generates
sudo systemctl enable haveged          #system entropy; this speeds up critical

sudo rm -fr /etc/pacman.d/gnupg        #operations in cryptographic programs such as gnupg
sudo pacman-key --init                 #(including the generation of new keyrings).
sudo pacman-key --populate archlinux
sudo systemctl stop haveged




sudo pacman -Sc                        #clean cache and combine it with cron
                                       #https://ostechnix.com/recommended-way-clean-package-cache-arch-linux/



#delete old log files
enable systemd startup

sudo pacman -S krusader
sudo pacman- S kio-extras  #image preview for krusader

# install filezilla
flatpak install flathub org.filezillaproject.Filezilla

# run filezilla
flatpak run org.filezillaproject.Filezilla






# old
https://raw.githubusercontent.com/portainer/templates/master/templates-2.0.json

#new
https://raw.githubusercontent.com/pi-hosted/pi-hosted/master/template/portainer-v2-arm64.json



sudo nano /home/ben/.ssh/known_hosts




#renaming multiple files
num=0; for i in *; do mv "$i" "$(printf '%04d' $num).${i#*.}"; ((num++)); done






